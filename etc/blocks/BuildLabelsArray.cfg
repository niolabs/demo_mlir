{
    "exclude": false,
    "fields": [
        {
            "formula": "{{ [[1 if r == $index else 0 for r in range(5)]] }}",
            "title": "labels"
        },
        {
            "formula": "{{ [$bottleneck] }}",
            "title": "batch"
        }
    ],
    "log_level": "NOTSET",
    "name": "BuildLabelsArray",
    "type": "Modifier",
    "version": "1.0.0"
}