{
    "exclude": false,
    "fields": [
        {
            "formula": "{{ [1 if r == $name_to_index[$new_label] else 0 for r in range(len($name_to_index))] }}",
            "title": "label"
        },
        {
            "formula": "{{ $bottleneck }}",
            "title": "batch"
        }
    ],
    "id": "buildlabelsarray_2",
    "log_level": "NOTSET",
    "name": "buildlabelsarray_2",
    "type": "Modifier",
    "version": "1.0.0"
}