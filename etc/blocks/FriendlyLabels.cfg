{
    "exclude": false,
    "fields": [
        {
            "formula": "{{ [{'label': [$index_to_name[str(i)] for i, p in enumerate($prediction[0]) if p == max($prediction[0])][0], 'confidence': round([p for p in $prediction[0] if p == max($prediction[0])][0] * 100, 2)}] if $index_to_name else [None] }}",
            "title": "learned"
        }
    ],
    "log_level": "NOTSET",
    "name": "FriendlyLabels",
    "type": "Modifier",
    "version": "1.0.0"
}