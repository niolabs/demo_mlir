{
    "exclude": false,
    "fields": [
        {
            "formula": "{{ [{'label': $index_to_name[i], 'confidence': round(p * 100, 2)} for i, p in enumerate($prediction[0])] if $index_to_name else [] }}",
            "title": "learned"
        }
    ],
    "id": "FriendlyLabels",
    "log_level": "NOTSET",
    "name": "FriendlyLabels",
    "type": "Modifier",
    "version": "1.0.0"
}